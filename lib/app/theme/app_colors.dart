import 'package:flutter/material.dart';

class AppColors {
  static const Color statusBarColor = Colors.transparent;
  static const Color white = Colors.white;
  static const Color primaryColor = Color.fromARGB(255, 27, 25, 25);
  static const Color primaryColorWithOpacity = Color.fromRGBO(27, 25, 25, 0.3);
  static const Color secondaryColor = Colors.transparent;
}
