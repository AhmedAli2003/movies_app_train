import 'package:flutter/material.dart';

class DetailsListMoviesScreen extends StatelessWidget {
  const DetailsListMoviesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
